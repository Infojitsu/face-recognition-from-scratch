# ============================================================
# Personal & training data -- NEVER commit
# Face photos are personal data; the celebrity images belong
# to a public dataset that must not be redistributed here.
# See README ("Training data") for how to obtain/collect them.
# The app recreates these folders automatically at startup.
# ============================================================
faces/
head_images/

# Derived artifacts (computed from the data above)
hog_vectors/
classifiers/
*.hog
*.dat

# ============================================================
# Build output
# ============================================================
bin/
*.class

# ============================================================
# Large third-party binaries -- install locally instead
# OpenCV jar + native library: see README ("OpenCV setup")
# ============================================================
lib/*.jar
lib/*.dll
lib/*.so
lib/*.dylib
!lib/README.txt

# ============================================================
# Course-assignment files (not part of the public project)
# ============================================================
proiect*.pdf

# ============================================================
# Romanian-language docs + generated javadoc
# Translate and selectively re-add later if desired.
# ============================================================
docs/

# ============================================================
# OS / editor noise
# ============================================================
Thumbs.db
Desktop.ini
.DS_Store
.idea/
.vscode/
*.iml
